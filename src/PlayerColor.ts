export enum PlayerColor {
    Red,
    Blue,
    Green,
    Yellow,
    Magenta,
    Black
}

export const ALL_PLAYER_COLORS: PlayerColor[] = [PlayerColor.Red, PlayerColor.Blue, PlayerColor.Green, PlayerColor.Yellow, PlayerColor.Magenta, PlayerColor.Black];